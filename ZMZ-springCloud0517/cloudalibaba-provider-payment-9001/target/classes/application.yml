server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
        metadata:
          version: 1.2.0

management:
  endpoints:
    web:
      exposure:
        include: '*'